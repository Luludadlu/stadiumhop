import { NextResponse } from "next/server";

export async function GET() {
  // Placeholder — Google Maps API integration in Phase 2
  return NextResponse.json({
    routes: [],
    message: "Transit detail API — coming in Phase 2",
  });
}
